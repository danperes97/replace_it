require "spec_helper"

describe ReplaceIt do
  it "has a version number" do
    expect(ReplaceIt::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
